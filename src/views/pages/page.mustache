{{>partials/header}}

{{#banner}}

<section class="banner" style="background-image: url('img/{{ background }}')">{{ title }}Todos os Produtos</section>

    <h2>{{ title }}Todos os produtos disponíveis</h2>
    {{/banner}}

    <div class="container list">
    {{#list}}
      <div class="item">
        <img src="img/{{ image }}" class="item-image" />
        <div class="item-name">{{ name }}</div>
        <div class="item-color">{{ color }}</div>
      </div>
      {{/list}}
    </div>

    {{>partials/footer}}